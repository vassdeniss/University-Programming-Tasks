#include <iostream>

int main() 
{
    std::cout << "Lexeme:" << std::endl;
    std::cout << "Set of words having the same basic meaning, " << std::endl
            << "differing only in gender, number, case, grammatical tense, etc." << std::endl;

    std::cout << "Literal:" << std::endl;
    std::cout << "Constant value of a variable." << std::endl;

    std::cout << "Command:" << std::endl;
    std::cout << "Specific instruction given to a computer " << std::endl
            << "application to perform some task or function" << std::endl;

    return 0;
}
